export interface NewCategory {
    name:string,
    sizeOptions:number[]
}

export interface ICategory{
    name: string,
    sizeOptions: number[],
    _id: string,
    createdAt: string,
    updatedAt: string,
    __v: number   
}

